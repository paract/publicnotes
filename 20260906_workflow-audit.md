# publicnotes 作業フロー点検

## 確認した原因と修正

- 長時間停止: 過去の実行履歴ではネットワーク権限の承認待ちが発生していた。
  APIの処理時間と承認待ちは別。処理のタイムアウトだけでは承認待ちは短縮できない。
  ローカル作業を先に完了し、ネットワーク処理を最後にまとめる。
- 作業場所: チャットの作業フォルダは空。実際のリポジトリは
  HANDOFF.md に記載した既存checkout。毎回の広範な探索は不要。
- Git起動: 31記事で31回の履歴取得を1回に集約。JSDOMも記事ごとに閉じる。
- 検査: 従来の npm run check は生成・上書き処理だった。
  読み取り専用に変更し、一覧が古ければエラーで通知する。
- 抜け漏れ: 紹介文なし・禁止語・280文字超過・戻るリンク不備は、
  prepare-note / publish-note / check でエラーにする。解析失敗を無視して公開しない。
- 無駄な更新: 出力内容が同じなら index.html を書き直さない。
  更新時は一時ファイルから置換する。
- 戻るリンク: 31記事の属性内の二重引用符を修正。負の余白も除去し、
  44pxの操作領域を確保。雛形にも戻るリンクを追加。
- 公開: 記事・画像のパスを明示し、無関係な変更はステージしない。
  未選択のコンテンツ変更や既存ステージがあれば自動コミットを止める。
  push失敗後はコミットを保ち、同じコマンドで再送できる。
- 公開確認: 現在のHEADのPages実行だけを照会。空のレスポンス・HTTPエラー・
  失敗・処理中を区別する。無限ポーリングや自動force pushは行わない。

## 次回の標準手順

1. AGENTS.mdとGit状態を確認。
2. 本文HTML、紹介文の順で作成。雛形の戻るリンクも付ける。
3. npm run publish-note -- notes/対象.html
4. npm run deploy-status

図解は依頼されたときだけ実施。通常の記事追加ではテスト全件や全ページの
目視確認は繰り返さない。コード変更時は npm test を実行する。
ローカル作業のみなら npm run prepare-note。後からの再検査は npm run check。

publish-note はこのリポジトリの main / notes / assets の日常運用向け。
汎用CLIの publicnotes init / build は維持し、publicnotes check を追加した。
今回npmレジストリへの公開や認証設定の変更は行っていない。

## 実測・検証

同一環境・31記事の単回計測: 修正前917ms・Git31回 → 修正後317ms・Git1回。
キャッシュや負荷で変動する値であり、文章作成や承認・デプロイ時間は含まない。
既存記事の並びと index.html の内容は変化なし。

自動テスト5件:
- 検査の読み取り専用性・無変更ビルド・紹介文と導線の不備検出
- Gitの旧方式と新方式の時刻一致、一括編集後の並び
- 7件ページ送り、31記事のナビゲーション属性
- push失敗からの再送、重複コミット防止、無関係ファイルとステージの保護
- HEAD指定のAPI照会、空応答・HTTPエラーの扱い

ブラウザでのローカルHTML表示はURLポリシーによりブロックされたため、
スクリーンショットでの見た目確認は未実施。DOM解析とクリック処理のテストで検証した。
